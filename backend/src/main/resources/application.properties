# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/judges_management
spring.datasource.username=postgres
spring.datasource.password=harish
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server Configuration (WildFly will handle port)
# server.port=8080

# Application context path (optional - WildFly will use WAR name by default)
# server.servlet.context-path=/judges-management 